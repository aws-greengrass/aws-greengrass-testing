---
RecipeFormatVersion: "2020-01-25"
ComponentName: broken_soft_dep
ComponentVersion: "1.0.0"
ComponentDescription: broken_soft_dep
ComponentPublisher: Amazon
Manifests:
  - Platform:
      os: windows
    Lifecycle:
      run: >-
        powershell -command "& { echo \"Hello with Soft Dependency\"; exit 1 }"
  - Platform:
      os: "*"
      runtime: "*"
    Lifecycle:
      run: |-
        NEW_MESSAGE="Hello with Soft Dependency"
        echo $NEW_MESSAGE
        exit 1
